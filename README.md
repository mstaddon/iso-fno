# Isotropic Fourier Neural Operator
This model implements an isotropic options into the Fourier Neural Operator which ensures the model respects symmetries of space. If the input sample is rotated or flipped, then the corresponding output will also be rotated or flipped.
